/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: ["lh3.googleusercontent.com", "avatars.githubusercontent.com"],
  },
  // Préparer pour l'expansion internationale
  i18n: undefined, // sera activé plus tard
};

export default nextConfig;
